import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <button className="btn btn-active btn-primary">Button</button>
    </div>
  );
};

export default Home;
